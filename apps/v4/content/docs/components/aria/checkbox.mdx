---
title: チェックボックス
description: ユーザーがチェック済みとチェックなしを切り替えることができるコントロール。
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Checkbox
  api: https://react-aria.adobe.com/Checkbox#api
---

<ComponentPreview
  styleName="aria-nova"
  name="checkbox-demo"
  previewClassName="h-80"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add checkbox
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="checkbox"
  title="components/ui/checkbox.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

```tsx
<Checkbox />
```

## チェックされた状態

制御されていないチェックボックスには `defaultSelected` を使用するか、`isSelected` および
`onChange` は状態を制御します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [checked, setChecked] = React.useState(false)

  return <Checkbox isSelected={checked} onChange={setChecked} />
}
```

## 無効な状態

チェックボックスの `isInvalid` とフィールド ラッパーの `data-invalid` を
は無効なスタイルを示します。

<ComponentPreview styleName="aria-nova" name="checkbox-invalid" />

## 基本

適切なレイアウトとラベル付けを行うには、チェックボックスを `Field` および `FieldLabel` と組み合わせます。

<ComponentPreview styleName="aria-nova" name="checkbox-basic" />

## 説明

ヘルパー テキストとして `FieldContent` および `FieldDescription` を使用します。

<ComponentPreview styleName="aria-nova" name="checkbox-description" />

## 無効

`isDisabled` プロパティを使用して相互作用を防止し、無効なスタイルの `<Field>` コンポーネントに `data-disabled` 属性を追加します。

<ComponentPreview styleName="aria-nova" name="checkbox-disabled" />

## グループ

複数のフィールドを使用してチェックボックス リストを作成します。

<ComponentPreview styleName="aria-nova" name="checkbox-group" />

## テーブル

<ComponentPreview
  styleName="aria-nova"
  name="checkbox-table"
  previewClassName="p-4 md:p-8"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="checkbox-rtl"
  direction="rtl"
  previewClassName="h-80"
/>

## API リファレンス

詳細については、[React Aria](https://react-aria.adobe.com/Checkbox#api) ドキュメントを参照してください。
