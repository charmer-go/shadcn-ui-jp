---
title: アラートダイアログ
description: 重要なコンテンツでユーザーを中断し、応答を期待するモーダル ダイアログ。
featured: true
base: aria
component: true
links:
  doc: https://react-aria.adobe.com/Modal
  api: https://react-aria.adobe.com/Modal#api
---

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-demo"
  previewClassName="h-56"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add alert-dialog
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-aria-components
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="alert-dialog"
  title="components/ui/alert-dialog.tsx"
  styleName="aria-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx showLineNumbers
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/components/ui/alert-dialog"
```

```tsx showLineNumbers
<AlertDialogTrigger>
  <Button variant="outline">Show Dialog</Button>
  <AlertDialog>
    <AlertDialogHeader>
      <AlertDialogTitle>Are you absolutely sure?</AlertDialogTitle>
      <AlertDialogDescription>
        This action cannot be undone. This will permanently delete your account
        from our servers.
      </AlertDialogDescription>
    </AlertDialogHeader>
    <AlertDialogFooter>
      <AlertDialogCancel>Cancel</AlertDialogCancel>
      <AlertDialogAction>Continue</AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialog>
</AlertDialogTrigger>
```

## 構成

次の構成を使用して、`AlertDialog` を構築します。

```text
AlertDialogTrigger
├── Button
└── AlertDialog
    ├── AlertDialogHeader
    │   ├── AlertDialogMedia
    │   ├── AlertDialogTitle
    │   └── AlertDialogDescription
    └── AlertDialogFooter
        ├── AlertDialogCancel
        └── AlertDialogAction
```

## 基本

タイトル、説明、キャンセルボタンと続行ボタンを備えた基本的なアラートダイアログ。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-basic"
  previewClassName="h-56"
/>

## 小さい

`size="sm"` プロップを使用して、アラート ダイアログを小さくします。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-small"
  previewClassName="h-56"
/>

## メディア

`AlertDialogMedia` コンポーネントを使用して、アイコンや画像などのメディア要素をアラート ダイアログに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-media"
  previewClassName="h-56"
/>

## メディアを使用した小規模な

`size="sm"` プロパティを使用してアラート ダイアログを小さくし、`AlertDialogMedia` コンポーネントを使用してアイコンや画像などのメディア要素をアラート ダイアログに追加します。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-small-media"
  previewClassName="h-56"
/>

## 破壊的

`AlertDialogAction` コンポーネントを使用して、警告ダイアログに破壊的なアクション ボタンを追加します。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-destructive"
  previewClassName="h-56"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="aria-nova"
  name="alert-dialog-rtl"
  direction="rtl"
  previewClassName="h-56"
/>

## API リファレンス

### サイズ

`AlertDialogContent` コンポーネントの `size` プロパティを使用して、アラート ダイアログのサイズを制御します。次の値を受け入れます。

|小道具 |タイプ |デフォルト |
| ------ | ------------------- | ----------- |
| `size` | `"default" \| "sm"` | `"default"` |

他のコンポーネントとそのプロパティの詳細については、[React Aria ドキュメント](https://react-aria.adobe.com/Modal#api) を参照してください。
