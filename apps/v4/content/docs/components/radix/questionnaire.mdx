---
title: アンケート
description: 単一選択、複数選択、自由入力、スキップ可能な質問に対応した複数ステップのアンケート。
base: radix
component: true
---

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-demo"
  previewClassName="min-h-[560px] p-4 sm:p-8"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add questionnaire
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install @shadcn/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="questionnaire"
  title="components/ui/questionnaire.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import {
  Questionnaire,
  QuestionnaireActions,
  QuestionnaireChoice,
  QuestionnaireChoices,
  QuestionnaireDescription,
  QuestionnaireError,
  QuestionnaireInput,
  QuestionnaireItem,
  QuestionnaireNext,
  QuestionnairePrevious,
  QuestionnaireProgress,
  QuestionnaireSkip,
  QuestionnaireSubmit,
  QuestionnaireTitle,
} from "@/components/ui/questionnaire"
```

```tsx
const items = [
  {
    name: "direction",
    required: true,
    prompt: "What should we prototype next?",
    description: "Choose a direction or write your own.",
    choices: [
      {
        value: "delegation",
        label: "Delegation",
        description: "Show how work moves to a specialist.",
      },
      {
        value: "questions",
        label: "Question prompts",
        description: "Show choices while the interface waits.",
      },
      { value: "both", label: "Both together" },
    ],
    input: { label: "Another answer", placeholder: "Type another answer…" },
  },
  {
    name: "detail",
    required: false,
    prompt: "How much detail should it include?",
    description: "Skip this if you are not sure yet.",
    choices: [
      { value: "focused", label: "Focused" },
      { value: "complete", label: "Complete flow" },
    ],
  },
] as const
```

コレクションを一度定義します。サーバーでレンダリングされる
の進行状況、アクション、およびショートカットをパーツにマップします。

```tsx
<Questionnaire items={items} onSubmit={handleSubmit}>
  <QuestionnaireProgress />
  {items.map((question) => (
    <QuestionnaireItem
      key={question.name}
      name={question.name}
      required={question.required}
    >
      <QuestionnaireTitle>{question.prompt}</QuestionnaireTitle>
      <QuestionnaireDescription>
        {question.description}
      </QuestionnaireDescription>
      <QuestionnaireChoices>
        {question.choices.map((choice) => (
          <QuestionnaireChoice key={choice.value} value={choice.value}>
            <span className="font-medium">{choice.label}</span>
            {"description" in choice ? (
              <span className="text-muted-foreground">
                {choice.description}
              </span>
            ) : null}
          </QuestionnaireChoice>
        ))}
        {"input" in question ? (
          <QuestionnaireInput
            aria-label={question.input.label}
            placeholder={question.input.placeholder}
          />
        ) : null}
      </QuestionnaireChoices>
      <QuestionnaireError />
    </QuestionnaireItem>
  ))}
  <QuestionnaireActions>
    <QuestionnairePrevious />
    <QuestionnaireSkip />
    <QuestionnaireNext />
    <QuestionnaireSubmit />
  </QuestionnaireActions>
</Questionnaire>
```

```tsx
function handleSubmit(event: React.FormEvent<HTMLFormElement>) {
  event.preventDefault()
  const answers = new FormData(event.currentTarget)
  // answers.get("direction"), answers.getAll(...) for multiple items.
}
```

## 構成

```text
Questionnaire
├── QuestionnaireProgress
├── QuestionnaireItem
│   ├── QuestionnaireTitle
│   ├── QuestionnaireDescription
│   ├── QuestionnaireChoices
│   │   ├── QuestionnaireChoice
│   │   └── QuestionnaireInput
│   └── QuestionnaireError
└── QuestionnaireActions
    ├── QuestionnairePrevious
    ├── QuestionnaireSkip
    ├── QuestionnaireNext
    └── QuestionnaireSubmit
```

Questionnaire owns the ordered items, active item, answer state, validation,
の進行状況とナビゲーションに焦点が当てられます。これを含むページ、カード、ダイアログ、またはドロワーが
クローズとキャンセルの動作、永続化、トランスポート、および分岐。

## サーバー レンダリング

Pass `items` to server-render the active item, progress, actions, and answer
shortcuts. See the
[headless Questionnaire](/docs/react/questionnaire) を参照してください。

## 複数選択

Use `multiple` for an item that accepts more than one fixed answer.

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-multiple"
  previewClassName="min-h-[420px] p-4 sm:p-8"
/>

## フリーフォームの回答

ユーザーが別の回答を提供できる場合は、固定の選択肢を使用して `QuestionnaireInput` を作成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-freeform"
  previewClassName="min-h-[420px] p-4 sm:p-8"
/>

## 明示的スキップ

オプションの項目が意図的に未回答のままになっている可能性がある場合は、`QuestionnaireSkip` を追加します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-skip"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## ショートカット

`shortcuts` を使用して、各回答に文字または数字キーを割り当てます。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-shortcuts"
  previewClassName="min-h-[480px] p-4 sm:p-8"
/>

## カスタム検証

制御されたナビゲーションと Zod などの外部スキーマを組み合わせて、無効な項目に戻り、そのエラーを表示します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-validation"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 制御

無効なステップに戻るなど、ホスト状態からアクティブ項目を制御します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-controlled"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 再開

Restore a saved active item and default answers, then reset changes back to that saved state.

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-resume"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## 条件付きアイテム

Disable 項目も受け入れます。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-conditional"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## ナビゲーション状態

アイテムのステータスを読み取り、無効なナビゲーションとカスタム アクション スタイルをオプトインします。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-navigation-state"
  previewClassName="min-h-[480px] p-4 sm:p-8"
/>

## カスタム進行状況

進行状況レンダリング状態を使用して、カスタム進行状況インジケーターを作成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-progress"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## アニメーション アイテムを所有しています

進行状況とナビゲーションを静止させたまま、アクティブな項目をアニメーション化します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-animated"
  previewClassName="min-h-[520px] p-4 sm:p-8"
/>

## カード

Compose Questionnaire with Card slots while keeping the question title and description semantic.

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-card"
  previewClassName="min-h-[560px] p-4 sm:p-8"
/>

## ダイアログ

キャンセルと閉じる操作をホスト側で管理しながら、ダイアログ内にアンケートを構成します。

<ComponentPreview
  align="end"
  styleName="radix-nova"
  name="questionnaire-dialog"
  previewClassName="min-h-[320px] p-4 sm:p-8"
/>

## アクセシビリティ

`QuestionnaireItem` は `fieldset` をレンダリングし、`QuestionnaireTitle` はその
`legend`。説明とアクティブなエラーは現在のアイテムに関連付けられています。
と無効な項目および応答コントロールにより `aria-invalid` が公開されます。

修正された選択肢は、ネイティブのラジオとチェックボックスの動作を保持します。進行状況が公開されます
、ナビゲーションは実際のボタンを使用し、非アクティブなアイテムと
アクションは非表示で不活性です。ナビゲーションが成功すると、新しくアクティブになった
item; failed validation focuses an available answer control.

Always give `QuestionnaireInput` an accessible name with a visible label,
`aria-label`、または `aria-labelledby`。プレースホルダーはラベルではありません。
[Questionnaire accessibility guide](/docs/react/questionnaire#accessibility)
for labeling custom compositions and the complete keyboard behavior.

## スタイルなし

The behavior in `Questionnaire` comes from the `@shadcn/react` package. To use
it directly with your own markup and styles, see
[Questionnaire](/docs/react/questionnaire) under @shadcn/react.

## API リファレンス

The props, data attributes, and render states for every part are documented on
[@shadcn/react Questionnaire](/docs/react/questionnaire#api-reference) ページ。
スタイル付きコンポーネントは、対応するスタイルされていないプロパティを継承します。ナビゲーション
コンポーネントは、Button `size` および `variant` プロパティ、およびユーザーの以前の回答に適用されない
`QuestionnaireActions` は、スタイルのみのレイアウト ヘルパーです。
