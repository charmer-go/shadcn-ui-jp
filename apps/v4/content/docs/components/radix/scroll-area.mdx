---
title: スクロール領域
description: ネイティブのスクロール機能を拡張し、ブラウザー間で一貫したカスタム スタイルを提供します。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/scroll-area
  api: https://www.radix-ui.com/docs/primitives/components/scroll-area#api-reference
---

<ComponentPreview
  styleName="radix-nova"
  name="scroll-area-demo"
  previewClassName="h-96"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add scroll-area
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install radix-ui
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="scroll-area"
  title="components/ui/scroll-area.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { ScrollArea, ScrollBar } from "@/components/ui/scroll-area"
```

```tsx showLineNumbers
<ScrollArea className="h-[200px] w-[350px] rounded-md border p-4">
  Your scrollable content here.
</ScrollArea>
```

## 構成

次の構成を使用して、`ScrollArea` を構築します。

```text
ScrollArea
└── ScrollBar
```

## 水平

Use `ScrollBar` with `orientation="horizontal"` for horizontal scrolling.

<ComponentPreview styleName="radix-nova" name="scroll-area-horizontal-demo" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview
  styleName="radix-nova"
  name="scroll-area-rtl"
  direction="rtl"
  previewClassName="h-auto"
/>

## APIリファレンス

[基数 UI スクロール領域](https://www.radix-ui.com/docs/primitives/components/scroll-area#api-reference) ドキュメントを参照してください。
