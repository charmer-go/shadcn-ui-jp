---
title: サイズ変更可能
description: キーボードサポートを備えた、サイズ変更可能なパネルグループとレイアウトにアクセス可能。
base: radix
component: true
links:
  doc: https://github.com/bvaughn/react-resizable-panels
  api: https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels
---

<ComponentPreview
  styleName="radix-nova"
  name="resizable-demo"
  previewClassName="h-80"
/>

## 概要

`Resizable` コンポーネントは、[bvaughn](https://github.com/bvaughn) によって [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels) の上に構築されています。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add resizable
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします。</Step>

```bash
npm install react-resizable-panels
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="resizable"
  title="components/ui/resizable.tsx"
  styleName="radix-nova"
/>

<Step>プロジェクト設定に合わせてインポートパスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@/components/ui/resizable"
```

```tsx showLineNumbers
<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel>One</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel>Two</ResizablePanel>
</ResizablePanelGroup>
```

## 構成

次の構成を使用して、`ResizablePanelGroup` を構築します。

```text
ResizablePanelGroup
├── ResizablePanel
├── ResizableHandle
└── ResizablePanel
```

## 垂直

垂直方向のサイズ変更には `orientation="vertical"` を使用します。

<ComponentPreview styleName="radix-nova" name="resizable-vertical" />

## ハンドル

`ResizableHandle` の `withHandle` プロップを使用して、表示されるハンドルを表示します。

<ComponentPreview styleName="radix-nova" name="resizable-handle" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl)を参照してください。

<ComponentPreview styleName="radix-nova" name="resizable-rtl" direction="rtl" />

## APIリファレンス

[react-resizable-panels](https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels) ドキュメントを参照してください。

## 変更ログ

### 2025-02-02 `react-resizable-panels` v4

`react-resizable-panels` v4 に更新されました。詳細については、[v4.0.0 リリース ノート](https://github.com/bvaughn/react-resizable-panels/releases/tag/4.0.0) を参照してください。

If you're using `react-resizable-panels` primitives directly, note the following changes:

| v3                           | v4                      |
| ---------------------------- | ----------------------- |
| `PanelGroup` | `Group` |
| `PanelResizeHandle` | `Separator` |
| `direction` prop             | `orientation` prop      |
| `defaultSize={50}`           | `defaultSize="50%"`     |
| `onLayout`                   | `onLayoutChange`        |
| `ImperativePanelHandle` | `PanelImperativeHandle` |
| `ref` prop on Panel          | `panelRef` prop         |
| `data-panel-group-direction` | `aria-orientation` |

<Callout>
  shadcn/ui ラッパー コンポーネント (`ResizablePanelGroup`、`ResizablePanel`、名前付きプログレスバーとしての
  `ResizableHandle`) は変更されません。
</Callout>
