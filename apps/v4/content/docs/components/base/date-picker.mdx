---
title: Date Picker
description: 範囲とプリセットを備えた日付ピッカー コンポーネント。 Vaul の代わりに
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="date-picker-demo" />

## インストール

日付ピッカーは、`<Popover />` コンポーネントと `<Calendar />` コンポーネントの組み合わせを使用して構築されます。

See installation instructions for the [Popover](/docs/components/base/popover#installation) and the [Calendar](/docs/components/base/calendar#installation) components.

## 使用法

```tsx showLineNumbers title="components/example-date-picker.tsx"
"use client"

import * as React from "react"
import { cn } from "cn"
import { format } from "date-fns"
import { Calendar as CalendarIcon } from "lucide-react"

import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date>()

  return (
    <Popover>
      <PopoverTrigger
        render={
          <Button
            variant="outline"
            data-empty={!date}
            className="justify-start text-left font-normal data-[empty=true]:text-muted-foreground"
          />
        }
      >
        <CalendarIcon />
        {date ? format(date, "PPP") : <span>Pick a date</span>}
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0">
        <Calendar mode="single" selected={date} onSelect={setDate} />
      </PopoverContent>
    </Popover>
  )
}
```

詳細については、[React DayPicker](https://react-day-picker.js.org) ドキュメントを参照してください。

## 構成

A date picker is built from `Popover` and `Calendar` (there is no `DatePicker` root component):

```text
Popover
├── PopoverTrigger
└── PopoverContent
    └── Calendar
```

## 基本

A basic date picker component.

<ComponentPreview styleName="base-nova" name="date-picker-basic" />

## 範囲ピッカー

日付の範囲を選択するための日付ピッカー コンポーネント。

<ComponentPreview styleName="base-nova" name="date-picker-range" />

## Date of Birth

内の子孫 生年月日を選択するための日付ピッカー コンポーネント。このコンポーネントには、日付と月を選択するためのドロップダウン キャプション レイアウトが含まれています。

<ComponentPreview styleName="base-nova" name="date-picker-dob" />

## 入力

A date picker component with an input field for selecting a date.

<ComponentPreview styleName="base-nova" name="date-picker-input" />

## タイム ピッカー

などの属性も公開します。 時刻を選択するための時刻入力フィールドを備えた日付ピッカー コンポーネント。

<ComponentPreview styleName="base-nova" name="date-picker-time" />

## 自然言語ピッカー

このコンポーネントは、`chrono-node` ライブラリを使用して自然言語日付を解析します。

<ComponentPreview styleName="base-nova" name="date-picker-natural-language" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="date-picker-rtl"
  direction="rtl"
/>
