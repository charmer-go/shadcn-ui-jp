---
title: チェックボックス
description: ユーザーがチェック済みとチェックなしを切り替えることができるコントロール。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/checkbox
  api: https://base-ui.com/react/components/checkbox#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="checkbox-demo"
  previewClassName="h-80"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">マニュアル</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add checkbox
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>次の依存関係をインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>次のコードをコピーしてプロジェクトに貼り付けます。</Step>

<ComponentSource
  name="checkbox"
  title="components/ui/checkbox.tsx"
  styleName="base-nova"
/>

<Step>プロジェクト設定に一致するようにインポート パスを更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用法

```tsx
import { Checkbox } from "@/components/ui/checkbox"
```

```tsx
<Checkbox />
```

## Checked State

制御されていないチェックボックスには `defaultChecked` を使用するか、`checked` および
`onCheckedChange` 状態を制御します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [checked, setChecked] = React.useState(false)

  return <Checkbox checked={checked} onCheckedChange={setChecked} />
}
```

## 無効な状態

Set `aria-invalid` on the checkbox and `data-invalid` on the field wrapper to
は、無効なスタイルを表示します。

<ComponentPreview styleName="base-nova" name="checkbox-invalid" />

## 基本

Pair the checkbox with `Field` and `FieldLabel` for proper layout and labeling.

<ComponentPreview styleName="base-nova" name="checkbox-basic" />

## Description

Use `FieldContent` and `FieldDescription` for helper text.

<ComponentPreview styleName="base-nova" name="checkbox-description" />

## 無効

を定義します。 `disabled` プロパティを使用して相互作用を防止し、無効なスタイルの `<Field>` コンポーネントに `data-disabled` 属性を追加します。

<ComponentPreview styleName="base-nova" name="checkbox-disabled" />

## グループ

複数のフィールドを使用してチェックボックス リストを作成します。

<ComponentPreview styleName="base-nova" name="checkbox-group" />

## テーブル

<ComponentPreview
  styleName="base-nova"
  name="checkbox-table"
  previewClassName="p-4 md:p-8"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 構成ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="checkbox-rtl"
  direction="rtl"
  previewClassName="h-80"
/>

## API リファレンス

詳細については、[ベース UI](https://base-ui.com/react/components/checkbox#api-reference) ドキュメントを参照してください。
